<section class="submit bg-white">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="submit-wrapper">
                    <div class="d-flex submit-content m-4 justify-content-between align-items-center flex-column flex-md-row text-center text-md-left">
                        <% if @input.blank? %>
                            <span>😶</span>
                          <% else %>
                            <br><h2>"<%= @input %>"</h2>
                          <% end %>
                        </p>
                        <hr>
                        <h2><span>Score: <%= @score %></span>
                        </h2>
                    </div>
                  <%= link_to "Ask a new question", home_path %>
                </div>
            </div>
        </div>
    </div>
</section>
